Fitness Tracker
===============

TODO - Flesh out this README with human-friendly information about the project purpose, and how to run the application.
       Include a reference to the original "fitnessjiffy-spring" repo, and explain the project history.

TODO - Review the README.md files in the "backend" and "web" directories.

TODO - Remove the `migration` directory, and the `docker` symlink, before squash-merging this branch into "main".

TODO - Also parameterize out the "steveperkins.com" hardcoded hostname values in the "web" directory, and make hosting
       dynamically configurable in `backend/src/main.ts` (it looks like we've already parameterized it out of the web
       frontend, by having the docker build command insert it at build time).
